[project]
name = "cosmogony-tools"
version = "0.1.0"
description = "Python helpers for Cosmogony migration and diagnostics"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

